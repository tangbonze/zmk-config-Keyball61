# Peripheral (left) half: key matrix + nice!view. The trackball sits on the
# right half, which is the central.

CONFIG_ZMK_BATTERY_REPORTING=y
CONFIG_ZMK_BATTERY_REPORTING_FETCH_MODE_STATE_OF_CHARGE=y
CONFIG_ZMK_SLEEP=y
# 15 minutes timeout (15 * 60 * 1000ms)
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=900000

# Relay so settings edited in DYA Studio reach this half as well.
CONFIG_ZMK_SPLIT_RELAY_EVENT=y
CONFIG_ZMK_SPLIT_RELAY_EVENT_DATA_LEN=240
CONFIG_ZMK_CUSTOM_SETTINGS=y
CONFIG_ZMK_CUSTOM_SETTINGS_SPLIT_RPC_RELAY=y
CONFIG_ZMK_SETTINGS_RPC=y
CONFIG_ZMK_SETTINGS_SAVE_DEBOUNCE=10000
CONFIG_ZMK_LOW_PRIORITY_THREAD_STACK_SIZE=2048

# Zephyr 4.1 enables the console by default; a keyboard does not need it.
CONFIG_CONSOLE=n
